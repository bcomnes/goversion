rules:
  dependabot-cooldown:
    config:
      days: 1
  unpinned-uses:
    config:
      policies:
        # Official GitHub actions and CodeQL use major tags for compatible security updates.
        "actions/*": ref-pin
        "github/codeql-action/*": ref-pin
        # First-party bcomnes actions use moving major references for compatible updates.
        "bcomnes/*": ref-pin
        # Require immutable references for every other third-party action.
        "*": hash-pin
